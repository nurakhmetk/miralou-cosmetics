'use strict';

/**
 * about-hero-banner service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::about-hero-banner.about-hero-banner');
